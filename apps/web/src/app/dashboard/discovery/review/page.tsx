/**
 * Renders nothing of its own — see `discovery/map/page.tsx`'s docstring.
 * This file exists only so `/dashboard/discovery/review` is a real,
 * directly-navigable route.
 */
export default function DiscoveryReviewPage() {
  return null;
}
